{
  "name": "collateralized-loan-project",
  "version": "1.0.0",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:test": "hardhat run --network sepolia-testnet scripts/deploy.js",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.4"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "ethers": "^6.13.1"
  }
}
